return {
  'jbyuki/nabla.nvim',
  config = function()
    --     vim.cmd [[
    -- nnoremap <leader>pe :lua require("nabla").popup()<CR> " Customize with popup({border = ...})  : `single` (default), `double`, `rounded`
    --     ]]
    -- require('nabla').enable_virt {
    --   autogen = true, -- auto-regenerate ASCII art when exiting insert mode
    --   silent = true, -- silents error messages
    -- }
  end,
}
